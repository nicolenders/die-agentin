BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Publication] ADD [language] NVARCHAR(1000),
[repoUrl] NVARCHAR(2048);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

