BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Mission] ADD [slidesFileName] NVARCHAR(1000),
[slidesFilePath] NVARCHAR(2048);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

